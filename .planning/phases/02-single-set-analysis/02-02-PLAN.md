---
phase: 02-single-set-analysis
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - dashboard/components/gauge_chart.py
  - dashboard/components/timeline_chart.py
  - dashboard/components/waveform_chart.py
  - tests/test_chart_builders.py
autonomous: true
requirements: [VIZ-01, VIZ-02, VIZ-04, ANAL-03]

must_haves:
  truths:
    - "Gauge charts display FINA green/yellow/red zone coloring with metric value and deduction label"
    - "Phase timeline shows horizontal stacked bar with 3 colored segments labeled in Chinese"
    - "IMU waveform chart shows 3 traces (accel blue, gyro orange, tilt purple) on time axis"
    - "Fusion dual-axis chart overlays vision angle (green, left Y) and IMU tilt (blue, right Y) with correlation"
    - "All charts use CHART_THEME from dashboard/components/__init__.py"
  artifacts:
    - path: "dashboard/components/gauge_chart.py"
      provides: "build_gauge() and build_scoring_card() functions returning Plotly figures"
      exports: ["build_gauge", "build_scoring_card"]
    - path: "dashboard/components/timeline_chart.py"
      provides: "build_phase_timeline() returning Plotly horizontal stacked bar"
      exports: ["build_phase_timeline"]
    - path: "dashboard/components/waveform_chart.py"
      provides: "build_imu_waveform() and build_fusion_chart() returning Plotly figures"
      exports: ["build_imu_waveform", "build_fusion_chart"]
    - path: "tests/test_chart_builders.py"
      provides: "Unit tests verifying chart builder output structure"
  key_links:
    - from: "dashboard/components/gauge_chart.py"
      to: "dashboard/core/scoring.py"
      via: "consumes MetricResult dataclass"
      pattern: "from dashboard\\.core\\.scoring import MetricResult"
    - from: "dashboard/components/gauge_chart.py"
      to: "dashboard/components/__init__.py"
      via: "imports CHART_THEME"
      pattern: "from dashboard\\.components import CHART_THEME"
    - from: "dashboard/components/waveform_chart.py"
      to: "plotly.subplots"
      via: "make_subplots for dual-axis fusion chart"
      pattern: "from plotly\\.subplots import make_subplots"
---

<objective>
Build all Plotly chart builder functions that visualize the scoring engine output.

Purpose: Chart builders are pure functions that take structured data (MetricResult, phase dicts, numpy arrays) and return Plotly figures. No Streamlit dependency -- these are importable by the training page (Plan 04) and testable in isolation.

Output: 3 new component modules (gauge_chart.py, timeline_chart.py, waveform_chart.py) + 1 test file.
</objective>

<execution_context>
@/Users/billthechurch/.claude/get-shit-done/workflows/execute-plan.md
@/Users/billthechurch/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-single-set-analysis/02-CONTEXT.md
@.planning/phases/02-single-set-analysis/02-RESEARCH.md
@.planning/phases/02-single-set-analysis/02-UI-SPEC.md
@.planning/phases/02-single-set-analysis/02-01-SUMMARY.md

<interfaces>
<!-- Types from Plan 01 that chart builders consume -->

From dashboard/core/scoring.py (created in Plan 01):
```python
@dataclass
class MetricResult:
    name: str           # Chinese display name (e.g., "腿部垂直偏差")
    value: float        # Raw metric value
    unit: str           # "deg", "ratio", "score"
    deduction: float    # FINA deduction (0.0, 0.2, 0.5)
    zone: str           # "clean", "minor", "major"
    max_value: float    # Gauge range max

@dataclass
class SetReport:
    metrics: list[MetricResult]   # 5 metrics
    overall_score: float          # 10.0 - sum(deductions)
    phases: list[dict]            # [{name, start, end, zone_color}]
    correlation: float | None     # IMU-vision correlation coefficient
```

From dashboard/components/__init__.py (existing):
```python
CHART_THEME = {
    "template": "plotly_white",
    "font_family": "Source Sans Pro, sans-serif",
    "font_color": "#262730",
    "title_font_size": 16,
    "axis_title_font_size": 14,
    "axis_tick_font_size": 11,
    "legend_font_size": 12,
    "annotation_font_size": 12,
    "margin": {"l": 32, "r": 16, "t": 24, "b": 24},
    "paper_bgcolor": "rgba(0,0,0,0)",
    "plot_bgcolor": "rgba(0,0,0,0)",
    "colorway": ["#0068C9", "#09AB3B", "#FACA2B", "#FF4B4B", "#83C9FF", "#FF8C00", "#7D44CF"],
}
```

FINA zone colors (from UI-SPEC):
- Clean (green): #09AB3B
- Minor (yellow): #FACA2B
- Major (red): #FF4B4B

IMU trace colors (from UI-SPEC):
- Accelerometer: #0068C9 (blue)
- Gyroscope: #FF8C00 (orange)
- Fused tilt: #7D44CF (purple)

Fusion chart colors (from UI-SPEC):
- Vision angle: #09AB3B (green, left Y-axis)
- IMU tilt: #0068C9 (blue, right Y-axis)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Gauge, timeline, and waveform chart builders with tests</name>
  <files>dashboard/components/gauge_chart.py, dashboard/components/timeline_chart.py, dashboard/components/waveform_chart.py, tests/test_chart_builders.py</files>
  <read_first>
    - dashboard/components/__init__.py (CHART_THEME dict with all keys)
    - dashboard/core/scoring.py (MetricResult and SetReport dataclass definitions from Plan 01)
    - .planning/phases/02-single-set-analysis/02-UI-SPEC.md (Gauge Visual Specification, Phase Timeline Visual Specification, IMU Waveform Chart Specification, Fusion Dual-Axis Chart Specification)
    - .planning/phases/02-single-set-analysis/02-RESEARCH.md (Code Examples section: Gauge Chart, Fusion Dual-Axis Chart patterns)
    - dashboard/config.py (load_config for FINA threshold access)
  </read_first>
  <behavior>
    Tests for chart builders:
    - test_build_gauge_returns_figure: build_gauge returns plotly.graph_objects.Figure
    - test_build_gauge_has_indicator: figure data[0] is type "indicator"
    - test_build_gauge_height_200: layout.height == 200
    - test_build_gauge_three_steps: gauge.steps has 3 entries (green/yellow/red zones)
    - test_build_scoring_card_returns_list: build_scoring_card returns list of 5 figures
    - test_build_phase_timeline_returns_figure: returns go.Figure
    - test_build_phase_timeline_three_traces: figure has 3 bar traces
    - test_build_phase_timeline_horizontal: traces have orientation "h"
    - test_build_phase_timeline_height_40: layout.height == 40
    - test_build_imu_waveform_returns_figure: returns go.Figure
    - test_build_imu_waveform_three_traces: figure has 3 scatter traces
    - test_build_imu_waveform_height_300: layout.height == 300
    - test_build_fusion_chart_returns_tuple: returns (go.Figure, float|None)
    - test_build_fusion_chart_two_traces: figure has 2 scatter traces
    - test_build_fusion_chart_height_350: layout.height == 350
    - test_build_fusion_chart_correlation: correlation is a float between -1 and 1
  </behavior>
  <action>
    **TDD RED phase:** Create tests/test_chart_builders.py with all tests. Tests construct MetricResult instances and synthetic numpy arrays to feed into chart builders.

    **TDD GREEN phase:** Implement the three chart builder modules:

    **dashboard/components/gauge_chart.py:**
    - `build_gauge(metric: MetricResult, config: dict, target: float | None = None) -> go.Figure`:
      Read thresholds from config["fina"]. Create go.Indicator(mode="gauge+number") with:
      - number font size 28, family "Source Sans Pro"
      - title text = metric.name, font size 14
      - gauge axis range [0, metric.max_value], tickfont size 11
      - gauge bar color "#0068C9" (accent)
      - gauge steps: [{range: [0, clean_thresh], color: "#09AB3B"}, {range: [clean_thresh, minor_thresh], color: "#FACA2B"}, {range: [minor_thresh, max_value], color: "#FF4B4B"}]
      - threshold line at target value if provided (color "#262730", width 2, thickness 0.75)
      - Layout: height=200, margin={"l":0,"r":0,"t":24,"b":0}, paper_bgcolor="rgba(0,0,0,0)", font_family from CHART_THEME, font_color from CHART_THEME
    - `build_scoring_card(metrics: list[MetricResult], config: dict) -> list[go.Figure]`:
      Return [build_gauge(m, config) for m in metrics]

    **dashboard/components/timeline_chart.py:**
    - `build_phase_timeline(phases: list[dict]) -> go.Figure`:
      Create go.Figure(). For each phase dict (keys: name, start, end, zone_color):
      - Add go.Bar(y=["阶段"], x=[end - start], name=phase["name"], orientation="h", marker_color=phase["zone_color"], text=phase["name"], textposition="inside", textfont={"size": 12, "color": "#FFFFFF"})
      - Layout: barmode="stack", height=40, margin={"l":0,"r":0,"t":0,"b":0}, showlegend=False, xaxis title="时间 (秒)" with title_font_size 14, yaxis visible=False
      - Apply CHART_THEME template, font_family, font_color, paper_bgcolor, plot_bgcolor

    **dashboard/components/waveform_chart.py:**
    - `build_imu_waveform(time: np.ndarray, accel_mag: np.ndarray, gyro_mag: np.ndarray, tilt_angle: np.ndarray) -> go.Figure`:
      3 traces: go.Scatter for accel (blue #0068C9, width 1.5, name "加速度"), gyro (orange #FF8C00, width 1.5, name "角速度"), tilt (purple #7D44CF, width 2, name "倾斜角"). Layout: height=300, xaxis_title="时间 (秒)", legend orientation "h" yanchor "bottom" y=1.02. Apply CHART_THEME.
    - `build_fusion_chart(time: np.ndarray, vision_angle: np.ndarray, imu_tilt: np.ndarray) -> tuple[go.Figure, float | None]`:
      Use make_subplots(specs=[[{"secondary_y": True}]]). Add vision trace (green #09AB3B, width 2, name "视觉关节角度", secondary_y=False). Add IMU trace (blue #0068C9, width 2, name "IMU 倾斜角", secondary_y=True). Layout: height=350, xaxis_title="时间 (秒)", legend orientation "h". Left Y: title "视觉关节角度 (°)", title_font_color "#09AB3B". Right Y: title "IMU 倾斜角 (°)", title_font_color "#0068C9". Compute correlation: mask = ~np.isnan(vision_angle) & ~np.isnan(imu_tilt); corr = float(np.corrcoef(vision_angle[mask], imu_tilt[mask])[0,1]) if mask.sum() > 10 else None. Return (fig, corr).
  </action>
  <verify>
    <automated>cd /Users/billthechurch/Downloads/test_rec && source .venv/bin/activate && python -m pytest tests/test_chart_builders.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "def build_gauge" dashboard/components/gauge_chart.py
    - grep -q "def build_scoring_card" dashboard/components/gauge_chart.py
    - grep -q "go.Indicator" dashboard/components/gauge_chart.py
    - grep -q "#09AB3B" dashboard/components/gauge_chart.py
    - grep -q "def build_phase_timeline" dashboard/components/timeline_chart.py
    - grep -q "orientation.*h" dashboard/components/timeline_chart.py
    - grep -q "barmode.*stack" dashboard/components/timeline_chart.py
    - grep -q "def build_imu_waveform" dashboard/components/waveform_chart.py
    - grep -q "def build_fusion_chart" dashboard/components/waveform_chart.py
    - grep -q "make_subplots" dashboard/components/waveform_chart.py
    - grep -q "secondary_y" dashboard/components/waveform_chart.py
    - grep -q "np.corrcoef" dashboard/components/waveform_chart.py
    - grep -q "from dashboard.components import CHART_THEME" dashboard/components/gauge_chart.py
    - python -m pytest tests/test_chart_builders.py -x -q exits 0
  </acceptance_criteria>
  <done>All chart builders return correctly structured Plotly figures. Gauges have 3 FINA zone color steps. Timeline shows 3 horizontal stacked bars. IMU waveform has 3 traces with correct colors. Fusion chart has dual Y-axes with correlation coefficient. All tests pass.</done>
</task>

</tasks>

<verification>
cd /Users/billthechurch/Downloads/test_rec && source .venv/bin/activate && python -m pytest tests/test_chart_builders.py -x -v
</verification>

<success_criteria>
- gauge_chart.py produces go.Indicator figures with FINA zone coloring (green/yellow/red steps)
- timeline_chart.py produces horizontal stacked bar with 3 phase segments
- waveform_chart.py produces 3-trace IMU chart and dual-axis fusion chart with correlation
- All charts apply CHART_THEME styling consistently
- All unit tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-single-set-analysis/02-02-SUMMARY.md`
</output>
